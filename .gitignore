# ── Node / JavaScript ─────────────────────────────────────────────────────────
node_modules/
dist/
.angular/
.nest/
.npm/
.yarn/
yarn.lock
pnpm-lock.yaml

# Packed npm tarballs
*.tgz
*.tar.gz

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Rust / Soroban ────────────────────────────────────────────────────────────
target/
# Cargo.lock is excluded for general packages…
Cargo.lock
# …but committed for the contracts workspace so CI builds don't re-resolve
# to incompatible versions of soroban-sdk 26 stack dependencies.
!contracts/Cargo.lock

# Compiled WASM artifacts
*.wasm

# ── Test Artifacts ────────────────────────────────────────────────────────────
# Coverage reports
coverage/
__snapshots__/
**/__snapshots__/
*.snap
*.test-snapshot
junit.xml
test-results/
.nyc_output/
lcov.info
*.lcov
coverage-final.json
jest-coverage/

# Snapshot files (Jest, Vitest, Jasmine, Soroban SDK)
__snapshots__/
*.snap
test_snapshots/
**/test_snapshots/

# Test result / reporter output
junit.xml
test-results/
karma-test-results.xml
browser_test_results/
jasmine-results/
playwright-report/
cypress/screenshots/
cypress/videos/
cypress/downloads/
# Kiro AI artifacts
.kiro/
# Codespace / devcontainer
.devcontainer/
# Local override files — not for source control
*.local
*.local.*

# Cache directories used by test runners
.vitest-cache/
.jest-cache/
.jest_cache/
.karma/

# ── Build Artifacts ───────────────────────────────────────────────────────────
build/
*.js.map
*.d.ts.map
*.tsbuildinfo

# shared/ package compiled output (source is index.ts)
shared/index.js
shared/index.d.ts

# ── IDE / Editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*.suo
*.ntvs*
*.njsproj
*.sln
*.bak
*.orig
*.rej
*~

# ── Soroban CLI / Deployment ──────────────────────────────────────────────────
.soroban/
.stellar/
contract-ids.testnet.json
scripts/contract-ids.testnet.json
contract-ids.*.json

# ── Docker / Runtime ──────────────────────────────────────────────────────────
docker-compose.override.yml
pids/
*.pid
*.seed
*.pid.lock

# ── Profiling ─────────────────────────────────────────────────────────────────
*.heapsnapshot
*.cpuprofile

# Soroban CLI local deploy artifacts
.stellar/
contract-ids.*.json

# OS artefacts
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor artefacts
*.code-workspace
.history/

