# ── Node / JavaScript ─────────────────────────────────────────────────────────
node_modules/
dist/
.angular/
.nest/

# Package managers
.npm/
.yarn/
yarn.lock
pnpm-lock.yaml

# ── Rust / Soroban ────────────────────────────────────────────────────────────
target/
Cargo.lock

# Kiro CLI
.kiro/

# shared/ package compiled output (source is index.ts)
shared/index.js
shared/index.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo
*.suo
*.ntvs*
*.njsproj
*.sln

# Soroban CLI state and deployment artefacts
.soroban/
contract-ids.testnet.json
scripts/contract-ids.testnet.json

# Soroban test snapshots (auto-generated, not source — never commit these)
test_snapshots/

# Test artifacts — auto-generated, must not be committed
coverage/
__snapshots__/
*.snap
junit.xml
test-results/
.nyc_output/
lcov.info
*.lcov
# Soroban test snapshots (auto-generated by soroban-sdk test framework)
test_snapshots/
**/test_snapshots/
# Karma / browser test output
karma-test-results.xml
browser_test_results/
# Vitest / Jest cache
.vitest-cache/
.jest-cache/
# Jest stale file
.jest_cache/
# Jest coverage directory
jest-coverage/
# Jasmine test output
jasmine-results/
# Angular test output
.karma/
# Playwright / Cypress artifacts
playwright-report/
cypress/screenshots/
cypress/videos/
cypress/downloads/

# ── Build Artifacts ───────────────────────────────────────────────────────────
build/
*.js.map
*.d.ts.map
build/
# Rust/Soroban compiled output
*.wasm
Cargo.lock
# The contracts workspace pins ed25519-dalek to the soroban-sdk 26 stack;
# commit its lockfile so CI builds don't re-resolve to incompatible versions.
!contracts/Cargo.lock

# Package manager
.npm/
.yarn/
yarn.lock
pnpm-lock.yaml
# Packed npm tarballs
*.tgz
*.tar.gz

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
*.bak
*.orig
# Editor temp files
*.bak
*.orig
*~

# TypeScript incremental build cache
*.tsbuildinfo

# Jest coverage XML (often generated by JUnit reporter)
coverage-final.json
# Editor backup files
*.orig
*.bak
*.rej

# TypeScript incremental build
*.tsbuildinfo

# Nest.js build cache
.nest/

# Docker Compose local overrides (not for sentinel staging config)
docker-compose.override.yml

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Profiling
*.heapsnapshot
*.cpuprofile

# Soroban CLI local deploy artifacts
.stellar/
contract-ids.*.json
